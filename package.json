{
  "name": "nocturneai",
  "version": "0.1.0",
  "private": true,
  "description": "Policy-first control plane for authorized security assessments.",
  "type": "module",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "test": "npm run build && node --test dist/tests/**/*.test.js",
    "start": "npm run build && node dist/apps/control-plane/src/cli.js"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0"
  }
}

